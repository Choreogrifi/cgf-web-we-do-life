export type NavItem = {
  label: string;
  href: string;
};

export type Service = {
  title: string;
  description: string;
  icon: string;
  benefits: string[];
};

export type Testimonial = {
  content: string;
  author: string;
  role: string;
  image: string;
};

export type FAQItem = {
  question: string;
  answer: string;
};